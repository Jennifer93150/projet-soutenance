{% extends 'base.html.twig' %}

{% block title %}Troc'Eco | Recherche | Nos catégories{% endblock %}

{% block contenuPrincipal %}

<div>
    <h1 class="text-center">Recherche</h1>
</div>
<div class="container">
    <div class="row g-2">
            <!-- on boucle que sur le titre -->
            {% for categorie in noscategories %}

                    <div class="card col-lg-5 m-1">
                        <div class="card-body">
                            <h5 class="card-title text-center">{{ categorie.nom }}</h5>
                            
                        </div>
                        <div class="text-center">
                            <a href="{{ path('maison')}}" class="align-self-center justify-self-center">
                                <img src="..." class="card-img-bottom" alt="...">
                            </a>  
                        </div>
                        
                    </div>
            {% endfor %}
                        
        
    </div>
</div>
        
{% endblock %}